# Server
PORT=3001
NODE_ENV=development
TRUST_PROXY=false

# Database (Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/quiltplanner?schema=public"

# Auth
JWT_SECRET="change-me-to-a-long-random-string"

# Frontend / CORS
FRONTEND_URL="http://localhost:3000"

# Stripe
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# AI Providers
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""

# Admin / Debug (MVP security)
# Recommended: set ADMIN_API_KEY and use x-admin-key header for /api/admin/*
ADMIN_API_KEY=""

# Optional: only needed if you want to gate debug routes in non-prod with x-debug-key
DEBUG_API_KEY=""

# Optional: if you use admin-email fallback in requireAdmin middleware
ADMIN_EMAIL=""

# Email (Resend)
RESEND_API_KEY=""
